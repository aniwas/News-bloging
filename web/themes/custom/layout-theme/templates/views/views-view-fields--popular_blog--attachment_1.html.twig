
    {{ fields.field_image.content}}
    <div class="w-100 d-flex flex-column justify-content-center bg-light px-3" style="height: 100px; testvi">
        <div class="mb-1" style="font-size: 13px;">
            <a href="">{{ fields.field_categories.content }}</a>
            <span class="px-1">/</span>
            <span>{{ fields.changed.content}}</span>
        </div>
        <a class="h6 m-0" href="">{{ fields.title.content}}</a>
    </div>

